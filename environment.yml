name: kilonova
channels:
  - conda-forge
dependencies:
  - python=3.11
  # conda-forge-only binary dependency; everything pip-safe lives in pyproject.toml
  - galsim
  - pip
  - pip:
      - -e .[sim,dev]
